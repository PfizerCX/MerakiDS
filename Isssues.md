## Issues


- **Accordion:** We might want to do a different hover effect, is the line thickness change enough for accessibility?

- **Button:** We're using sm, md, lg for sizing when I think we're using small, medium, large in other instances. We should standardize that.

- **Callout:** Let's review the original design on this one. Seems off.

- **CardImage:** We use the term 'Emphasized' for the solid background color - I think this is the only place where we do that. Should rename to 'solid'.

- **TimePicker:** On selection, it's allowing the user to scroll when the time-select popout is open. Setting it to absolute position doesn't seem to help.
- **SegmentedControl:** On selection, the segments expand out with the icon. Not sure how to make it fixed in such a way where the icon width is included on unselected states.
- **SearchField:** No access to patterns file to modify Search fields/ no MCP.